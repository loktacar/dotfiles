Bash configuration

Files:
    - bashrc
        * Uses bash completion and colored terms if available.
        * Has a long list of personal aliases aswell as a special PATH
          declaration at the bottom.
        * Linked to '~/.bashrc'
    - git-completion.bash
        * Adds tab completion to git commands and arguments.
        * linked to '~/.git-completion.bash'
